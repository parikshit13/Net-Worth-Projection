import React from "react";
import ReactDOM from "react-dom/client";
import NetWorthProjection from "./NetWorthProjection.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NetWorthProjection />
  </React.StrictMode>
);
